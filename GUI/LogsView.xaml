<UserControl x:Class="GUI.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock HorizontalAlignment="Center"
                        Width="Auto"
                        Text="Logs"
                        FontWeight="Bold"
                        FontSize="50"
                        Foreground="Blue"
                        FontFamily="Book Antiqua"/>
            <Border BorderBrush="AliceBlue" HorizontalAlignment="Stretch">
                
            </Border>
            <DataGrid Height="Auto" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False"
                      CanUserResizeColumns="True" CanUserResizeRows="False" CanUserSortColumns="False" ColumnWidth="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Type" Width="Auto" />
                    <DataGridTextColumn Header="Message" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
